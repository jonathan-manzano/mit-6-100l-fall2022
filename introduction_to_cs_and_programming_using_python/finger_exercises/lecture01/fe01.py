# fe01.py

"""
Assume three variables are already defined for you: `a`, `b`, and `c`.
Create a variable called `total` that adds `a` and `b` then multiplies the
result by `c`. Include a last line in your code to print the value:
`print(total)`
"""

# Assume a, b, and c are already defined
a = 1
b = 2
c = 3

# Calculate total by adding a and b, then multiplying by c
total = (a + b) * c

# Print the value of total
print(total)
